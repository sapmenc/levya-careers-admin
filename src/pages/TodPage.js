import React from "react";
import Tod from "../components/TOD/Tod";

function TodPage({ textColor, setTextColor }) {
  return <Tod textColor={textColor} setTextColor={setTextColor} />;
}

export default TodPage;
